1. # What is error handling in Python?
Error handling is writing code that anticipates certain errors and responds to them while the program runs.
2. # What does try do?
try contains code that might produce an error.
3. # What does except do?
this is the exception conditional it handles the error that might come from ("try")

4. # What is a ValueError?
this is an error tht occurs when an inappropriate value for the code is used eg. using a string for an int operation

5. # When does the else block run?
else runs only if ther is no error

6. # When does the finally block run?
finally always run,it is the final block of code

7. # What is the difference between ValueError and TypeError?
value errors occur when the wrong type of value is used eg. using str where int is required while typeerror occurs when an inappropriate type for the operation is used

8. # Why is it better to catch a specific error like ValueError instead of using a bare except:?
so that the error is easier to pinpoint

9. # In our BankAccount program, why did we put the insufficient-funds check inside the else block rather than except?
because the except block of code is used to pinpoint errors but else is used when the code goes through or succeeds 

10. Complete this:

The biggest thing I learned about error handling today is.....
to be honest i think its a little difficlt to understand but i think it has helped me understand the layers that there are to error handling