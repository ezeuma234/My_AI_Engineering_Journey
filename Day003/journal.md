w# Day 003 Journal

## What I Learned

One important thing I learned today is the difference between assigning a value and comparing two things using `=` and `==`.

I also learned about variables, objects, memory, data types, `type()`, and `id()`.

## What I Found Most Interesting

The difference between `=` and `==` was the most interesting concept to me. It helped me understand that assigning a value and comparing values are two completely different operations.

## Challenges

I was initially confused about the `id()` function and how it relates to objects. After experimenting with different variables and lists, I understood that `id()` shows the identity of an object. Two variables can have the same value but refer to different objects, or they can refer to the same object.

## What I Want to Improve Tomorrow

I would like to improve my speed tomorrow while still making sure that I understand the concepts properly.