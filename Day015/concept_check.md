1. # What is a function?
A function is a reusable block of code designed to perform a specific task.

2. # What is a parameter?
A parameter is a placeholder variable defined when creating a function.
3. # What is an argument?
The actual value that is passed into a function when the function is called.
4. # What is the difference between a parameter and an argument?
a parameter is a placeholder while the argument is the actual useful part of the function,the real values being used

5. # What is the difference between print() and return?
return is able to perform actual functions,it can be used to call variables and parameters and other calculative functions while print() is just a display function

6. # Why can we store the result of a function in a variable when the function uses return?
because of returns ability to store values and perform calculations so its a good call function

7. # What is an f-string, and when would you use one?
aAn f-string allows you to insert variables or expressions directly into a string.
8. # Why did we use int(input(...)) in our age program?
to turn the input from the user from a string into an integer

9. # In our check_age() function, why didn't we need to write another condition inside else?
because the check age function was able to perform the task at hand

10. # Complete this:

The biggest thing I learned about functions and return today is...
how to properly call fuctions and make use of parameters and arguments