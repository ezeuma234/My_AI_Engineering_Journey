# WHAT IS A FUNCTION
a function is a reusable block of code that only runs when you call it
# WHY DO WE USE FUNCTIONS
because they make coding easier and they are reusable
# WHAT IS THE DIFFERENCE BETWEEN PARAMETER AND ARGUMENT
A parameter is like an order block,the way or order the code is supposed to output while arguments are what the code works with
# WHAT IS THE DIFFERENCE BETWEEN PRINT AND RETURN
print() displays a value on the screen.
return sends a value back to the code that called the function
# WHAT KEYWORD IS USED TO DEFINE A FUNCTION
def is used