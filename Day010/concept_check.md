1. # What is a dictionary?
A dictionary is a data structure that stores information as key-value pairs using curly braces {}.
2. # What is a key-value pair?
these are the data stored inside of a dictionary it conains a declared key and the value assigned to it
3. # How do you access a value in a dictionary?
you access a key value in a dictionary by calling the name of the key in order for it to print
4. # How do you add a new key-value pair?
you add a new key value pair by specifiying the name and then calling the name of the dictionary were treating eg. student["goal"] = "AI Engineer"
5. # How do you change an existing value?
we change an existing value by updating the values through the new key method
6. # What is the difference between del and .pop()?
del comes before the dictionary name while pop can only come after it eg del student["name] student.pop["name]
7. # How can you check if a key exists?
by calling the name in the dictionary if it is not there it displays a false value and this is because it makes use of boolean ("true or false")
8. # What does .items() do?
items are used in looping functions it helps list both the keys and value, the entire value pair
9. # How can you loop through a dictionary?
by the for function, for in
this is how it usually works
10. # Why are dictionaries useful?
Dictionaries are useful because they let us organize related information using meaningful keys.