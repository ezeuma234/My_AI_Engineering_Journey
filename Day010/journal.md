# Day 10 Journal

**What I learned:**
I learned about dictionaries and how they store information using key-value pairs. I also learned how to access, add, update, and remove dictionary data.

**What interested me:**
`.items()` interested me because it allows me to work with keys and values simultaneously. It makes looping through dictionaries elegant and easy to read.

**What confused me:**
The difference between `del` and `.pop()` confused me at first. Both can delete dictionary elements, but `del` only deletes the item, while `.pop()` deletes the item and returns its value.

**What I'll improve tomorrow:**
I want to improve my understanding of checking keys with `in` before accessing values. This will help me write safer code and avoid errors when a key does not exist.
