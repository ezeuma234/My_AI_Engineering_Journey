# WHAT IS A CLASS
A class is a template that defines the structure and attributes of an object, it is a bluprint for creating other objects
# WHAT IS AN OBJECT
An object is an object built from a class template
# WHAT DOES __INIT__ DO
This is a constructor function that stamps the default specifications of an object on every new one made
# WHAT IS SELF
A keyword in class that tells yu the exact object you are working on
# WHAT IS AN ATTRIBUTE
An attribute is data or a property belonging to an object.
# WHAT IS A METHOD
a method is a function that belongs to a class so it mut stay within the class, not an external indent
it contains only the name of dog1,because that is te only attribute it was given
im not too sure what bark does
because its calling the class name not the attribute and nothing has been assigned to it diectly
# the biggest thing i learned about OOP today is....
The biggest thing I learned about OOP today is how to use classes and the proper format for creating objects, including __init__, self, attributes, and methods.
# WHAT IS INHERITANCE
This is a programming pattern where a child class or a subclass automatically inherits or copies all of the parents class attributes and mthods whilst avoiding duplication
# WHAT IS A PARENT CLASS
This is the base class that provides all the starting attributes and methods for inheritance
# WHAT IS A CHILD CLASS
A child class or subclass is the specialized class that inherits from the parent whilst adding or modifying features
student is the parent class
# WHAT DOES SUPER() DO
Super() is a built in function that calls and executes a method from the parent class inside the parent class
it is making use of the super () and modifying the parents atributes whilst retaining the oroginal attributes,just adding the child attribute to it
# WHAT IS METHOD OVERIDING
This is the process of overdidng the parent class method with a custmized method from the child class
that keeps the original introduce method and its content from the parent class as well as using the super() to modify the method by adding its own
"Animal sound" and "Woof!"
# The biggest thing I learned about inheritance and super() is...
The biggest thing I learned about inheritance and super() is that a child class can reuse the parent's functionality and add its own functionality without rewriting the parent's code.