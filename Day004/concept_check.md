# WHAT IS AN OPERATOR
an operator is a tool used to perform numerical tasks with numbers such as "+" , "-" ,"*" etc they are used to perfor calculative tasks with numbers
# WHAT IS AN EXPRESSION
expressions are tools used to perform logical tasks with numbers and strings such as "and" , "or" etc
# WHAT IS THE DIFFERENCE BETTWEEN / AND //
/ is used for division while // is used for floor division. / returns a float while // returns an imteger
# WHAT DOES % DO
it is called modulus and it remturns the remainder of a division
# WHAT IS THE DIFFERENCE BETWEEN == AND !=
== mean that both comparisons are equal while != means that boh comparisons are not equal
# EXPLAIN AND,OR AND NOT
these are expressions that make use of true and false return values moat times but can still be used alongside numerical expressions. and evaluates both vaues but as long as one sie is false it will return a false value, while or compares the values but as long as on side is true it will retrn a true value as both sides do not need to be the same to be true
not returns the opposite value of what is put beside it,so if a value reads "not true" the return value will automatically be false