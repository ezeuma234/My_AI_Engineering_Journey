# WHAT IS AN IF STATEMENT
an if stateent is a conditional used in coding
# WHY IS IDENTATION IMPORTANT
Indentation helps your work look neater and tells your program what is coming next in your code
# WHAT IS THE DIFERENCE BETWEEN IF AND IF ELSE
if executes code only if its condition is True.
if...else always chooses between two possible paths:
one if the condition is True,
another if it is False.
# WHEN DO WE USE ELIF
we use elif when theres more than conditional statement to be compared as opposed to using just if and else
# WHAT HAPPENS WHEN THE FIRST IF CONDITION IS TRUE
It prints the initial proposed output